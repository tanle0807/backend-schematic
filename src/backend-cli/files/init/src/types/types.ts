/**
 * Enum to describe gender
 */
export enum TYPE_GENDER {
    male = 'MALE',
    female = 'FEMALE'
}

/**
 * Enum to code of order
 */
export enum CODE_ORDER {
    food = 'OF', // ORDER FOOD
    transport = 'OT', // ORDER TRANSPORT
    delivery = 'OD' // ORDER DELIVERY
}

